//! EngagementLinkTracker API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the EngagementLinkTracker CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// ENGAGEMENTLINKTRACKER TEST DATA GENERATOR
// ============================================================================

/// Test data generator for EngagementLinkTracker
pub struct EngagementLinkTrackerTestData;

impl TestDataGenerator for EngagementLinkTrackerTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "url": "https://example.com/test",
            "code": format!("TEST_{}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "title": null,
            "label": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "campaign_id": null,
            "medium_id": null,
            "source_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "url": "https://example.com/test",
            "code": format!("TEST_{}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "title": null,
            "label": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "campaign_id": null,
            "medium_id": null,
            "source_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// ENGAGEMENTLINKTRACKER API TEST
// ============================================================================

/// EngagementLinkTracker API test suite
pub struct EngagementLinkTrackerApiTest {
    inner: GenericCrudTest<EngagementLinkTrackerTestData>,
}

impl EngagementLinkTrackerApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/engagement_link_trackers", "EngagementLinkTracker");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, EngagementLinkTrackerTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for EngagementLinkTrackerApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_engagement_link_tracker_crud() {
        let mut test = EngagementLinkTrackerApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add EngagementLinkTracker specific tests here
// <<< CUSTOM TESTS END >>>
