//! GamificationBadgeUser API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the GamificationBadgeUser CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// GAMIFICATIONBADGEUSER TEST DATA GENERATOR
// ============================================================================

/// Test data generator for GamificationBadgeUser
pub struct GamificationBadgeUserTestData;

impl TestDataGenerator for GamificationBadgeUserTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "badge_id": Uuid::new_v4().to_string(),
            "recipient_user_id": Uuid::new_v4().to_string(),
            "sender_user_id": null,
            "grant_kind": "peer",
            "challenge_id": null,
            "comment": null,
            "level": null,
            "granted_at": now,
            "grant_key": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "badge_id": Uuid::new_v4().to_string(),
            "recipient_user_id": Uuid::new_v4().to_string(),
            "sender_user_id": null,
            "grant_kind": "peer",
            "challenge_id": null,
            "comment": null,
            "level": null,
            "granted_at": now,
            "grant_key": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// GAMIFICATIONBADGEUSER API TEST
// ============================================================================

/// GamificationBadgeUser API test suite
pub struct GamificationBadgeUserApiTest {
    inner: GenericCrudTest<GamificationBadgeUserTestData>,
}

impl GamificationBadgeUserApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/gamification_badge_users", "GamificationBadgeUser");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, GamificationBadgeUserTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for GamificationBadgeUserApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_gamification_badge_user_crud() {
        let mut test = GamificationBadgeUserApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add GamificationBadgeUser specific tests here
// <<< CUSTOM TESTS END >>>
