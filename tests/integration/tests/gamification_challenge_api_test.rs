//! GamificationChallenge API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the GamificationChallenge CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// GAMIFICATIONCHALLENGE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for GamificationChallenge
pub struct GamificationChallengeTestData;

impl TestDataGenerator for GamificationChallengeTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "state": "draft",
            "manager_user_id": null,
            "period": "once",
            "start_date": null,
            "end_date": null,
            "visibility_mode": "personal",
            "reward_badge_id": null,
            "reward_first_badge_id": null,
            "reward_second_badge_id": null,
            "reward_third_badge_id": null,
            "reward_failure": false,
            "reward_realtime": false,
            "report_frequency": "never",
            "last_report_date": null,
            "next_report_date": null,
            "remind_update_delay": null,
            "user_ids": json!({}),
            "include_all_users": false,
            "include_badge_ids": json!({}),
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "state": "draft",
            "manager_user_id": null,
            "period": "once",
            "start_date": null,
            "end_date": null,
            "visibility_mode": "personal",
            "reward_badge_id": null,
            "reward_first_badge_id": null,
            "reward_second_badge_id": null,
            "reward_third_badge_id": null,
            "reward_failure": false,
            "reward_realtime": false,
            "report_frequency": "never",
            "last_report_date": null,
            "next_report_date": null,
            "remind_update_delay": null,
            "user_ids": json!({}),
            "include_all_users": false,
            "include_badge_ids": json!({}),
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// GAMIFICATIONCHALLENGE API TEST
// ============================================================================

/// GamificationChallenge API test suite
pub struct GamificationChallengeApiTest {
    inner: GenericCrudTest<GamificationChallengeTestData>,
}

impl GamificationChallengeApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/gamification_challenges", "GamificationChallenge");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, GamificationChallengeTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for GamificationChallengeApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_gamification_challenge_crud() {
        let mut test = GamificationChallengeApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add GamificationChallenge specific tests here
// <<< CUSTOM TESTS END >>>
