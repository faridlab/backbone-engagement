//! EngagementSource REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{EngagementSourceService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateEngagementSourceDto, UpdateEngagementSourceDto, PatchEngagementSourceDto, EngagementSourceResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum EngagementSourceError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for EngagementSourceError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for EngagementSourceError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "ENGAGEMENTSOURCE_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "ENGAGEMENTSOURCE_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "ENGAGEMENTSOURCE_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "ENGAGEMENTSOURCE_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for EngagementSource.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /engagement_sources | List with pagination |
/// | POST | /engagement_sources | Create new |
/// | GET | /engagement_sources/:id | Get by ID |
/// | PUT | /engagement_sources/:id | Full update |
/// | PATCH | /engagement_sources/:id | Partial update |
/// | DELETE | /engagement_sources/:id | Soft delete |
/// | POST | /engagement_sources/bulk | Bulk create |
/// | POST | /engagement_sources/upsert | Upsert |
/// | GET | /engagement_sources/trash | List deleted |
/// | POST | /engagement_sources/:id/restore | Restore |
/// | DELETE | /engagement_sources/empty | Empty trash |
/// | GET | /engagement_sources/:id/deleted | Get deleted by ID |
/// | DELETE | /engagement_sources/trash/:id | Permanent delete from trash |
/// | GET | /engagement_sources/count | Count active entities |
/// | GET | /engagement_sources/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(EngagementSourceService::with_repository(repository));
/// let router = create_engagement_source_routes(service);
/// ```
pub fn create_engagement_source_routes(service: Arc<EngagementSourceService>) -> Router {
    BackboneCrudHandler::<EngagementSourceService, EngagementSource, CreateEngagementSourceDto, UpdateEngagementSourceDto, EngagementSourceResponseDto>::routes(
        service,
        "/engagement_sources",
    )
}

/// Create Axum router with only the read (GET) endpoints for EngagementSource.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_engagement_source_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_engagement_source_read_routes(service: Arc<EngagementSourceService>) -> Router {
    BackboneCrudHandler::<EngagementSourceService, EngagementSource, CreateEngagementSourceDto, UpdateEngagementSourceDto, EngagementSourceResponseDto>::read_routes(
        service,
        "/engagement_sources",
    )
}

/// Create Axum router with only the write (mutation) endpoints for EngagementSource.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
///
/// # This is unguarded generic CRUD, not a validated write path
///
/// These are plain create/update/patch/delete mutations over the entity row —
/// they bypass all business invariants. If the module exposes a validated write
/// service (e.g. a command router over its domain engine), serve THAT instead
/// for any mutation that must respect domain rules.
pub fn create_engagement_source_write_routes(service: Arc<EngagementSourceService>) -> Router {
    BackboneCrudHandler::<EngagementSourceService, EngagementSource, CreateEngagementSourceDto, UpdateEngagementSourceDto, EngagementSourceResponseDto>::write_routes(
        service,
        "/engagement_sources",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_engagement_source_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<EngagementSourceService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_engagement_source_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}
