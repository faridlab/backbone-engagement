//! GamificationChallengeMembership REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{GamificationChallengeMembershipService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateGamificationChallengeMembershipDto, UpdateGamificationChallengeMembershipDto, PatchGamificationChallengeMembershipDto, GamificationChallengeMembershipResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum GamificationChallengeMembershipError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for GamificationChallengeMembershipError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for GamificationChallengeMembershipError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "GAMIFICATIONCHALLENGEMEMBERSHIP_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "GAMIFICATIONCHALLENGEMEMBERSHIP_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "GAMIFICATIONCHALLENGEMEMBERSHIP_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "GAMIFICATIONCHALLENGEMEMBERSHIP_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for GamificationChallengeMembership.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /gamification_challenge_memberships | List with pagination |
/// | POST | /gamification_challenge_memberships | Create new |
/// | GET | /gamification_challenge_memberships/:id | Get by ID |
/// | PUT | /gamification_challenge_memberships/:id | Full update |
/// | PATCH | /gamification_challenge_memberships/:id | Partial update |
/// | DELETE | /gamification_challenge_memberships/:id | Soft delete |
/// | POST | /gamification_challenge_memberships/bulk | Bulk create |
/// | POST | /gamification_challenge_memberships/upsert | Upsert |
/// | GET | /gamification_challenge_memberships/trash | List deleted |
/// | POST | /gamification_challenge_memberships/:id/restore | Restore |
/// | DELETE | /gamification_challenge_memberships/empty | Empty trash |
/// | GET | /gamification_challenge_memberships/:id/deleted | Get deleted by ID |
/// | DELETE | /gamification_challenge_memberships/trash/:id | Permanent delete from trash |
/// | GET | /gamification_challenge_memberships/count | Count active entities |
/// | GET | /gamification_challenge_memberships/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(GamificationChallengeMembershipService::with_repository(repository));
/// let router = create_gamification_challenge_membership_routes(service);
/// ```
pub fn create_gamification_challenge_membership_routes(service: Arc<GamificationChallengeMembershipService>) -> Router {
    BackboneCrudHandler::<GamificationChallengeMembershipService, GamificationChallengeMembership, CreateGamificationChallengeMembershipDto, UpdateGamificationChallengeMembershipDto, GamificationChallengeMembershipResponseDto>::routes(
        service,
        "/gamification_challenge_memberships",
    )
}

/// Create Axum router with only the read (GET) endpoints for GamificationChallengeMembership.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_gamification_challenge_membership_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_gamification_challenge_membership_read_routes(service: Arc<GamificationChallengeMembershipService>) -> Router {
    BackboneCrudHandler::<GamificationChallengeMembershipService, GamificationChallengeMembership, CreateGamificationChallengeMembershipDto, UpdateGamificationChallengeMembershipDto, GamificationChallengeMembershipResponseDto>::read_routes(
        service,
        "/gamification_challenge_memberships",
    )
}

/// Create Axum router with only the write (mutation) endpoints for GamificationChallengeMembership.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
///
/// # This is unguarded generic CRUD, not a validated write path
///
/// These are plain create/update/patch/delete mutations over the entity row —
/// they bypass all business invariants. If the module exposes a validated write
/// service (e.g. a command router over its domain engine), serve THAT instead
/// for any mutation that must respect domain rules.
pub fn create_gamification_challenge_membership_write_routes(service: Arc<GamificationChallengeMembershipService>) -> Router {
    BackboneCrudHandler::<GamificationChallengeMembershipService, GamificationChallengeMembership, CreateGamificationChallengeMembershipDto, UpdateGamificationChallengeMembershipDto, GamificationChallengeMembershipResponseDto>::write_routes(
        service,
        "/gamification_challenge_memberships",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_gamification_challenge_membership_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<GamificationChallengeMembershipService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_gamification_challenge_membership_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}
