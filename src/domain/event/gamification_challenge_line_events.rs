//! Domain events for GamificationChallengeLine entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::GamificationChallengeLine;

/// Domain events for GamificationChallengeLine — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<GamificationChallengeLine>)` variant.
pub type GamificationChallengeLineEvent = CrudEvent<GamificationChallengeLine>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for GamificationChallengeLine events.
pub type GamificationChallengeLineEventPublisher = Arc<dyn CrudEventPublisher<GamificationChallengeLine> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopGamificationChallengeLineEventPublisher = NoOpCrudEventPublisher<GamificationChallengeLine>;
