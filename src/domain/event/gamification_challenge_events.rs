//! Domain events for GamificationChallenge entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::GamificationChallenge;

/// Domain events for GamificationChallenge — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<GamificationChallenge>)` variant.
pub type GamificationChallengeEvent = CrudEvent<GamificationChallenge>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for GamificationChallenge events.
pub type GamificationChallengeEventPublisher = Arc<dyn CrudEventPublisher<GamificationChallenge> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopGamificationChallengeEventPublisher = NoOpCrudEventPublisher<GamificationChallenge>;
