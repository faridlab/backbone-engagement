//! Repository trait for EngagementLinkTracker aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the EngagementLinkTracker aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::EngagementLinkTracker;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EngagementLinkTrackerPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl EngagementLinkTrackerPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct EngagementLinkTrackerPaginatedResult {
    pub data: Vec<EngagementLinkTracker>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EngagementLinkTrackerFilter {
    pub url: Option<String>,
    pub code: Option<String>,
    pub title: Option<String>,
    pub label: Option<String>,
    pub campaign_id: Option<Uuid>,
    pub medium_id: Option<Uuid>,
    pub source_id: Option<Uuid>,
}

impl EngagementLinkTrackerFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.url.is_some() || self.code.is_some() || self.title.is_some() || self.label.is_some() || self.campaign_id.is_some() || self.medium_id.is_some() || self.source_id.is_some()
    }
}

/// Repository trait for EngagementLinkTracker aggregate
///
/// This trait defines the persistence contract for EngagementLinkTracker entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait EngagementLinkTrackerRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new engagement_link_tracker entity
    async fn save(&self, entity: &EngagementLinkTracker) -> Result<EngagementLinkTracker>;

    /// Find engagement_link_tracker by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<EngagementLinkTracker>>;

    /// Find all engagement_link_tracker entities
    async fn find_all(&self) -> Result<Vec<EngagementLinkTracker>>;

    /// Update engagement_link_tracker by ID
    async fn update(&self, id: &str, entity: &EngagementLinkTracker) -> Result<Option<EngagementLinkTracker>>;

    /// Delete engagement_link_tracker by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List engagement_link_tracker with pagination
    async fn list(&self, params: EngagementLinkTrackerPaginationParams) -> Result<EngagementLinkTrackerPaginatedResult>;

    /// List engagement_link_tracker with pagination and filters
    async fn list_with_filters(&self, params: EngagementLinkTrackerPaginationParams, filters: EngagementLinkTrackerFilter) -> Result<EngagementLinkTrackerPaginatedResult>;

    /// Count all engagement_link_tracker entities
    async fn count(&self) -> Result<u64>;

    /// Count engagement_link_tracker entities matching filters
    async fn count_with_filters(&self, filters: EngagementLinkTrackerFilter) -> Result<u64>;

    /// Check if engagement_link_tracker exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete engagement_link_tracker by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted engagement_link_tracker by ID
    async fn restore(&self, id: &str) -> Result<Option<EngagementLinkTracker>>;

    /// List soft-deleted engagement_link_tracker entities
    async fn list_deleted(&self, params: EngagementLinkTrackerPaginationParams) -> Result<EngagementLinkTrackerPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save engagement_link_tracker entities
    async fn bulk_save(&self, entities: &[EngagementLinkTracker]) -> Result<Vec<EngagementLinkTracker>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
