//! GamificationGoal seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial GamificationGoal data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// GAMIFICATIONGOAL SEEDER
// ============================================================================

/// Seeder for gamification_goal
pub struct SeedGamificationGoalSeeder;

impl SeedGamificationGoalSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedGamificationGoalSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedGamificationGoalSeeder {
    fn name(&self) -> &'static str {
        "SeedGamificationGoalSeeder"
    }

    fn order(&self) -> i32 {
        9
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if engagement.gamification_goals table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM engagement.gamification_goals")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/gamification_goal_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM engagement.gamification_goals")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
