//! EngagementCampaign seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial EngagementCampaign data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ENGAGEMENTCAMPAIGN SEEDER
// ============================================================================

/// Seeder for engagement_campaign
pub struct SeedEngagementCampaignSeeder;

impl SeedEngagementCampaignSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedEngagementCampaignSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedEngagementCampaignSeeder {
    fn name(&self) -> &'static str {
        "SeedEngagementCampaignSeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if engagement.engagement_campaigns table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM engagement.engagement_campaigns")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/engagement_campaign_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM engagement.engagement_campaigns")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
