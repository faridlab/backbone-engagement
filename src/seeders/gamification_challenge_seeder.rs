//! GamificationChallenge seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial GamificationChallenge data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// GAMIFICATIONCHALLENGE SEEDER
// ============================================================================

/// Seeder for gamification_challenge
pub struct SeedGamificationChallengeSeeder;

impl SeedGamificationChallengeSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedGamificationChallengeSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedGamificationChallengeSeeder {
    fn name(&self) -> &'static str {
        "SeedGamificationChallengeSeeder"
    }

    fn order(&self) -> i32 {
        5
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if engagement.gamification_challenges table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM engagement.gamification_challenges")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/gamification_challenge_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM engagement.gamification_challenges")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
